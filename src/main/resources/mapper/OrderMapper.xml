<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ordersystem.mapper.OrderMapper">
    
    <sql id="baseColumns">
        id, order_no, user_id, total_amount, status, payment_method,
        payment_time, delivery_address, receiver_name, receiver_phone,
        remark, create_time, update_time, is_deleted
    </sql>
    
    <select id="findAll" parameterType="map" resultType="com.example.ordersystem.entity.Order">
        SELECT 
        <include refid="baseColumns"/>
        FROM orders
        WHERE is_deleted = 0
        <if test="orderNo != null and orderNo != ''">
            AND order_no LIKE CONCAT('%', #{orderNo}, '%')
        </if>
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="receiverName != null and receiverName != ''">
            AND receiver_name LIKE CONCAT('%', #{receiverName}, '%')
        </if>
        <if test="receiverPhone != null and receiverPhone != ''">
            AND receiver_phone = #{receiverPhone}
        </if>
        <if test="startTime != null">
            AND create_time >= #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
        ORDER BY create_time DESC
    </select>
    
    <select id="findByUserId" parameterType="map" resultType="com.example.ordersystem.entity.Order">
        SELECT 
        <include refid="baseColumns"/>
        FROM orders
        WHERE is_deleted = 0 AND user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="startTime != null">
            AND create_time >= #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
        ORDER BY create_time DESC
    </select>
    
</mapper>
